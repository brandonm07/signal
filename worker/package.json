{
  "name": "signal-pitcher",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev --config wrangler.toml",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply signaladvise --local --config wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply signaladvise --remote --config wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8",
    "wrangler": "^4.0.0"
  }
}
